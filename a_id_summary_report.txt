What I currently think happened

Based specifically on the screenshot, my leading working hypothesis is:

Something caused the QA login-service pod to be recreated today. The replacement pod attempted to pull the existing 1.0.128 image from JFrog. That image pull failed because the HTTPS connection to JFrog was reset. Consequently, the login-service pod never became Ready, leaving Istio without a healthy login-service upstream, which produced no healthy upstream when the QA OAuth authorization URL was accessed.

So I would investigate in this order:

pod creation/restart reason → image pull failure → JFrog/network/egress → endpoints → Istio, rather than starting with OAuth redirect configuration.

Send me the output of these three commands first:

kubectl get pods -n mics-nonprod-qa -o wide | findstr /I "login"
kubectl describe pod <login-pod-name> -n mics-nonprod-qa
kubectl get events -n mics-nonprod-qa --sort-by=.lastTimestamp | findstr /I "login pull image node kill evict"

With those, I can help you determine what caused the pod to be recreated on September 17 and why the image pull subsequently failed, instead of using another build as a temporary workaround.

Good. Now let’s fix this properly and permanently.

This is NOT a CI problem.
This is a **package-lock + JFrog integrity mismatch problem**.

Your error:

```
npm ERR! code EINTEGRITY
npm ERR! integrity checksum failed
wanted sha512-XXXX
but got sha512-YYYY
```

That means:

👉 The checksum stored in your `package-lock.json`
👉 Does NOT match the actual axios tarball currently stored in JFrog

So npm refuses to install it.

---

# 🔎 What Actually Happened

Earlier:

* JFrog had axios 1.13.2
* Your lock file had checksum for 1.13.2

Now:

* CI is trying to download axios 1.13.6
* But JFrog’s tarball content is different from what lock file expects
* So integrity fails

You manually edited line 5972.
❌ That was wrong.

You NEVER manually edit package-lock.json integrity.

---

# 🚨 IMPORTANT

Integrity field is cryptographic hash.

If tarball changes even 1 byte → checksum changes → npm fails.

You cannot fix this manually.

---

# ✅ CORRECT FIX (Enterprise Safe)

You MUST regenerate package-lock.json from a machine that can access JFrog correctly.

Since you have a UI developer with local setup, follow these exact steps.

---

# 🛠 STEP-BY-STEP FIX (Give This To UI Developer)

### 1️⃣ Delete lock and node_modules

```bash
rm -rf node_modules
rm -f package-lock.json
```

---

### 2️⃣ Clear npm cache

Very important.

```bash
npm cache clean --force
```

---

### 3️⃣ Verify npm registry

Run:

```bash
npm config get registry
```

It must show your JFrog URL:

```
https://cvsh.jfrog.io/artifactory/api/npm/cvs-aetnahealth-npm/
```

If not, check `.npmrc`.

---

### 4️⃣ Install Fresh

```bash
npm install
```

This will:

* Download axios 1.13.6 from JFrog
* Generate new integrity hash
* Create fresh package-lock.json

---

### 5️⃣ Verify axios version

Run:

```bash
npm list axios
```

Make sure it shows:

```
axios@1.13.6
```

---

### 6️⃣ Commit New Lock File

```bash
git add package-lock.json
git commit -m "Regenerate lock file with correct axios integrity"
git push
```

---

# ✅ That’s It

CI will now:

```
npm ci
```

And integrity will match.

Error gone.

---

# ⚠️ DO NOT EVER

* Manually edit integrity field
* Manually change resolved URL
* Modify tarball link manually
* Mix npm install and npm ci randomly

---

# 🧠 Why npm ci Failed

Because `npm ci`:

* Strictly uses package-lock.json
* Does NOT modify lock file
* Verifies integrity strictly

That is correct behavior.

---

# 🔒 Prevent This In Future (Important)

### Always:

* Developer runs `npm install`
* Commits updated package-lock.json
* CI runs only `npm ci`

Never modify lock file in CI.

---

# 🛡 Enterprise Hardening (Optional But Recommended)

Add this in CI before npm ci:

```yaml
- name: Verify lock file integrity
  run: |
    if ! git diff --exit-code package-lock.json; then
      echo "Lock file changed unexpectedly"
      exit 1
    fi
```

---

# 🎯 Root Cause Summary

Your JFrog tarball for axios changed.
Your lock file still had old checksum.
Integrity mismatch.
npm stopped install.

Correct behavior.

---

# 💡 Final Advice

If this still fails after regeneration:

Then your JFrog repo might be caching corrupted tarball.

In that case:

* Ask Artifactory admin to delete axios 1.13.6
* Re-upload or re-sync

But 99% of time regeneration fixes it.

---

If you want, I can now explain:

* Why caret (^) caused version shift
* Why 1.13.6 is selected
* Or how to fully pin axios forever

Tell me.

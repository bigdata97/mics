Steps to Follow
Open your GitHub Actions workflow file (e.g., .github/workflows/*.yaml)
Add the following permissions block at the top level of the workflow (just above jobs):
permissions:
  id-token: write
  contents: read
Save and commit the changes
Re-run the workflow to verify the issue is resolved

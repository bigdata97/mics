For a larger audience (developers, testers, business users), keep it factual and avoid too much technical detail.

**DEV URL Update**

The hostname update request has been completed for both DEV and QA environments.

As part of validation, we confirmed that the QA environment is functioning as expected and users can successfully authenticate and access the application using the new QA URL.

For DEV, all MICS-side configuration updates (Gateway, VirtualService, application configuration, and routing) have been verified and are aligned with the working QA setup. Based on our analysis, the DEV authentication flow is still referencing the previous redirect URI configuration in Ping, whereas the QA redirect URI has already been updated to the new hostname.

As a result:

* ✅ QA is fully accessible using the new hostname.
* ⚠️ DEV is currently unavailable after authentication due to the redirect URI mismatch.
* ✅ No additional code changes are currently identified on the MICS side.
* ⏳ Waiting for the DEV Ping redirect URI update to be completed.

Once the DEV redirect URI is updated, the DEV portal is expected to become accessible without requiring any additional application deployment.

Or a shorter version:

**DEV/QA Hostname Migration Status**

The new hostname updates have been successfully applied for both DEV and QA.

QA validation completed successfully and the portal is accessible using the new URL.

For DEV, our validation indicates that the new hostname configuration is in place; however, the Ping redirect URI still appears to be pointing to the previous DEV configuration. This is causing authentication requests to fail after redirection.

Current status:

* QA: ✅ Working with new hostname
* DEV: ⚠️ Awaiting Ping redirect URI update
* No additional code changes or redeployment are currently expected from the MICS side

We will provide another update once the Ping configuration change is completed.








===================================================================

This is probably better for your lead—short, evidence-based, and focused on the likely root cause.

Quick update on the DEV URL issue:

I compared the DEV configuration against QA and verified that the Gateway, VirtualService, and application configurations are aligned with the QA setup. I also validated the UI service through port-forwarding, and the application loads successfully.

During testing, I observed that the authentication flow is still redirecting to the old Ping authorization URL. Based on the network traces and port-forwarding validation, my current suspicion is that the DEV redirect URI/client configuration in Ping has not yet been updated to reflect the new DEV hostname.

At this point, I don't see any code or deployment issues on the MICS side. Once the Ping redirect URI is updated, the DEV portal should come up without any additional code changes. I also don't anticipate a redeployment being required since the latest configurations are already in place.

An even shorter version if you're sending it directly to your lead in Teams:

I validated the DEV setup against QA and all MICS-side configurations (Gateway, VirtualService, and app configs) appear to be correct. I also tested via port-forwarding and confirmed the UI loads successfully.

The authentication flow still appears to be redirecting to the old Ping authorization URL, so I suspect the DEV redirect URI/client configuration in Ping has not been updated yet.

Based on the current analysis, I don't see any additional code changes or redeployment needed from our side. Once the Ping configuration is updated, the DEV portal should start working as expected.

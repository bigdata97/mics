import logging

def init_logging_config():
    logging.basicConfig(
        level=logging.DEBUG,
        filename="logs/flashpoint.log",
        format="%(asctime)s - %(name)s - %(levelname)s - %(message)s"
    )
